import React from 'react';
import { DataDisplay } from '../DataDisplay/DataDisplay';
import './App.scss';

const App: React.FC = () => {
  return (
    <div className="App">
      <DataDisplay />
    </div>
  );
};

export default App;
